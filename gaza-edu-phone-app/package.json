{
  "name": "gaza-edu-phone-app",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first education app packaged for phone/tablet shells.",
  "scripts": {
    "serve": "npx serve www",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap add android && npx cap sync android",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
